<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="online.whitefieldmudra.aptmaintenance">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SigninActivity"
            android:label="@string/title_activity_signin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MaintenanceSubmitActivity"
            android:label="MaintenanceSubmit" />
        <activity
            android:name=".TenantVerificationActivity"
            android:label="TenantVerification" />
        <activity
            android:name=".BaseActivity"
            android:label="BaseActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ApproveRejectList"
            android:label="ApproveRejectList"
            />
        <activity
            android:name=".ResidentRegistration"
            android:label="ResidentRegistration"
             />
    </application>

</manifest>